import 'package:flutter/material.dart';

class ChatPostScreen extends StatelessWidget {
  const ChatPostScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
